package com.toraysoft.widget.locuspsw;

import android.content.Context;
import android.graphics.Canvas;
import android.util.AttributeSet;
import android.widget.TextView;

public class LocusTips extends TextView{
	public LocusTips(Context context) {
		super(context);
	}

	public LocusTips(Context context, AttributeSet attrs) {
		super(context, attrs);
	}

	public LocusTips(Context context, AttributeSet attrs, int defStyle) {
		super(context, attrs, defStyle);
	}
	
	@Override
	protected void onDraw(Canvas canvas) {
		super.onDraw(canvas);
	}
}
